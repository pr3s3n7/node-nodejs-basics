'use strict'

const { createFile, createFileSync } = require('fs-extra/lib/ensure/file')
const { createLink, createLinkSync } = require('fs-extra/lib/ensure/link')
const { createSymlink, createSymlinkSync } = require('fs-extra/lib/ensure/symlink')

module.exports = {
  // file
  createFile,
  createFileSync,
  ensureFile: createFile,
  ensureFileSync: createFileSync,
  // link
  createLink,
  createLinkSync,
  ensureLink: createLink,
  ensureLinkSync: createLinkSync,
  // symlink
  createSymlink,
  createSymlinkSync,
  ensureSymlink: createSymlink,
  ensureSymlinkSync: createSymlinkSync
}
